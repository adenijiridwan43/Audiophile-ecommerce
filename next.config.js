module.exports = {
  // Set turbopack.root to your repo root to avoid workspace-root inference warnings
  turbopack: {
    root: './',
  },
  // Optional: allow dev cross-origin (adjust if you still see cross-origin dev warnings)
  // allowedDevOrigins: ['http://172.21.208.1:3000'],
};
